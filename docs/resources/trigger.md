---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "agentops_trigger Resource - agentops"
subcategory: ""
description: |-
  A webhook trigger that fires a target agent or workflow when called.
---

# agentops_trigger (Resource)

A webhook trigger that fires a target agent or workflow when called.

## Example Usage

```terraform
# A webhook trigger that invokes an agent when called.
resource "agentops_trigger" "deploy" {
  name        = "deploy-webhook"
  description = "Fires the deploy agent on inbound webhooks"
  target_id   = "agent_01hxyz"
  target_type = "agent"
  is_enabled  = true
}

# The invocation token is returned only at create time; keep it secret.
output "deploy_trigger_token" {
  value     = agentops_trigger.deploy.token
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `description` (String) Free-form description.
- `header` (String) Name of the HTTP header carrying the signature. Server-assigned when omitted.
- `is_enabled` (Boolean) Whether the trigger is active. Defaults to `true`.
- `name` (String) Human-readable trigger name.
- `signing_credential_id` (String) ID of a credential used to sign/verify webhook payloads.
- `signing_secret` (String, Sensitive) Inline signing secret. Write-only; never returned by the API.
- `target_id` (String) ID of the agent or workflow this trigger invokes. Omit (together with `target_type`) to create a standalone webhook endpoint with no target — e.g. an endpoint an incident pipeline consumes. Changing this forces a new trigger.
- `target_type` (String) Kind of target (e.g. `agent`, `workflow`). Omit together with `target_id` for a standalone webhook endpoint. Changing this forces a new trigger.
- `webhook_type` (String) Webhook payload type. Defaults to the server's choice when omitted.

### Read-Only

- `created_at` (String) Creation timestamp.
- `id` (String) Trigger identifier.
- `token` (String, Sensitive) Webhook invocation token. Returned only at create and on rotation.
- `updated_at` (String) Last-update timestamp.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Triggers are imported by their id.
terraform import agentops_trigger.deploy trg_01hxyz
```
