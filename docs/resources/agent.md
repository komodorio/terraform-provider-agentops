---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "agentops_agent Resource - agentops"
subcategory: ""
description: |-
  A self-hosted KaOps agent. Creating this resource registers the agent with the control plane and returns a complete Helm values YAML and install command for deploying it in your own cluster. The install_values output is a ready-to-use values file for the agentops-agent-base Helm chart. Destroying the agent archives it first, then deletes it.
---

# agentops_agent (Resource)

A self-hosted KaOps agent. Creating this resource registers the agent with the control plane and returns a complete Helm values YAML and install command for deploying it in your own cluster. The `install_values` output is a ready-to-use values file for the `agentops-agent-base` Helm chart. Destroying the agent archives it first, then deletes it.



<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `agent_id` (String) Stable agent identifier (slug), unique within the account. Changing this forces a new agent.
- `instructions` (String) System prompt / instructions for the agent.

### Optional

- `credential_ref` (String) Name of the LLM credential configured on the account (not the secret value).
- `display_name` (String) Human-readable display name shown in the UI.
- `mcp_group_id` (String) MCP gateway group to bind to this agent. Can be changed without recreating the agent.
- `model` (String) LLM model the agent uses (e.g. `claude-opus-4-8`).

### Read-Only

- `created_at` (String) Creation timestamp.
- `install_command` (String) Helm install command returned by the API at creation time.
- `install_values` (String, Sensitive) Complete Helm values YAML returned by the API at creation time. Contains image coordinates, agent config, control-plane URL, and the worker token. Use as `-f values.yaml` with the `agentops-agent-base` chart, passing the worker token separately via `--set-string` if desired.
- `is_archived` (Boolean) Whether the agent has been archived.
- `name` (String) Display name as reported by the API.
- `status` (String) Agent status (`online`, `offline`, `archived`, etc.).
- `worker_token_hint` (String) Masked hint of the worker token (e.g. `wt_abc1...`).
